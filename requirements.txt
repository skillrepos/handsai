# --- LLM access ---
# OpenAI-compatible client used for BOTH Ollama (local, default) and Groq (optional, larger models)
openai>=1.30.0

# --- Model Context Protocol ---
# Official MCP Python SDK: FastMCP server + stdio client
mcp>=1.2.0

# --- Testing ---
# Used by the sample app's test suite and the Lab 8 evaluation harness
pytest>=8.0.0
