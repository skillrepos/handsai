# --- LLM access ---
# OpenAI-compatible client used for BOTH Ollama (local, default) and Groq (optional, larger models)
openai>=1.30.0

# --- Model Context Protocol ---
# Official MCP Python SDK 2.x: MCPServer + stdio client
# (2.0 renamed FastMCP -> MCPServer and snake_cased result fields; labs target 2.x)
mcp>=2.0,<3

# --- Testing ---
# Used by the inventory service's test suite and the Lab 8 evaluation harness
pytest>=8.0.0
